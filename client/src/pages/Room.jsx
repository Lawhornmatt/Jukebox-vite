import React from 'react'
import ReactDOM from 'react-dom'

const Room = () => {
    ReactDOM.render(
        <React.StrictMode>
            <h1>Welcome ot the room page</h1>
        </React.StrictMode>,
        document.getElementById('root')
    )
};

export default Room;